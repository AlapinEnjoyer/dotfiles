# Hyprpaper configuration (0.8.0+ syntax)
# Wallpapers are set dynamically via IPC by change-wallpaper.sh

ipc = true
splash = false

# Fallback wallpaper
wallpaper {
    monitor =
    path = ~/.config/backgrounds/misty-mountains.jpg
    fit_mode = cover
}
